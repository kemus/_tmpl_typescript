{
  "name": "{{ project_name_kebab }}",
  "version": "0.1.0",
  "description": "{{ project_description }}",
  "author": "{{ author }} <{{ author_email }}>",
  "license": "{{ license }}",
  "type": "module",
  "main": "dist/index.js",
  "scripts": {
    "build": "tsc",
{% if ts_test_framework == 'vitest' %}    "test": "vitest run",{% else %}    "test": "node --experimental-strip-types --test tests/index.test.ts",{% endif %}
    "lint": "eslint src/ tests/",
    "format": "prettier --write ."
  },
  "devDependencies": {
    "eslint": "^9",
    "prettier": "^3",
    "typescript": "^5",
{% if ts_test_framework == 'vitest' %}    "typescript-eslint": "^8",
    "vitest": "^3"{% else %}    "typescript-eslint": "^8"{% endif %}
  }
}
